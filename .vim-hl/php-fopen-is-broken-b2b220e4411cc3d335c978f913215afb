<span class="Special">&lt;?php</span>
set_error_handler(<span class="Statement">function</span>($errno, $errstr, $errfile, $errline) {
    <span class="Statement">throw</span> <span class="Statement">new</span> ErrorException(<span class="Constant">'Error!'</span>);
});

<span class="Statement">try</span> {
    $fp = fopen(<span class="Constant">'/etc/shadow'</span>, <span class="Constant">'r'</span>);
}
<span class="Statement">catch</span> (Exception $exc) {
    <span class="Statement">print</span>(<span class="Constant">&quot;Error!</span><span class="Special">\n</span><span class="Constant">&quot;</span>);
}
