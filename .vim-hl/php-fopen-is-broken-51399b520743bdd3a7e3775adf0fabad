<span class="Special">&lt;?php</span>
<span class="Statement">try</span> {
    $fp = <span class="Constant">null</span>;
    $fp = @fopen(<span class="Constant">'/etc/shadow'</span>, <span class="Constant">'r'</span>);
}
<span class="Statement">catch</span> (Exception $exc) { }

<span class="Statement">if</span> (!$fp) {
    $err = error_get_last();
    <span class="Statement">throw</span> <span class="Statement">new</span> Exception($err[<span class="Constant">'message'</span>]);
}
